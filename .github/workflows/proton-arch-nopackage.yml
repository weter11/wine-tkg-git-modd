name: Proton Master WoW64 CI - NO steamclient

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: archlinux:latest

    steps:
      - uses: actions/checkout@v7

      - name: Install Dependencies
        run: |
          # System-wide pacman wrapper to force --noconfirm across all subshells
          mv /usr/bin/pacman /usr/bin/pacman.real
          cat > /usr/bin/pacman <<'PACWRAP'
          #!/bin/bash
          exec /usr/bin/pacman.real --noconfirm "$@"
          PACWRAP
          chmod +x /usr/bin/pacman

          pacman -Syu --needed \
            base-devel \
            git \
            sudo \
            zip \
            patch \
            python \
            rsync \
            wget \
            curl \
            pkgconf \
            cmake \
            ninja \
            bison \
            flex \
            fontconfig \
            freetype2 \
            gnutls \
            libx11 \
            libxext \
            libxinerama \
            libxrandr \
            libxrender \
            libxcomposite \
            libxi \
            libxcursor \
            vulkan-icd-loader \
            vulkan-headers \
            mesa \
            wayland \
            wayland-protocols \
            fontforge \
            python-fonttools \
            mingw-w64-gcc

          # Setup non-root build user and transfer workspace permissions
          useradd -m user && echo "user ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers
          chown -R user:user "$GITHUB_WORKSPACE" /tmp

      - name: Configure & Compile Proton-tkg
        run: |
          sudo -u user GITHUB_WORKSPACE="$GITHUB_WORKSPACE" bash -c '
            set -eu
            cd "$GITHUB_WORKSPACE/proton-tkg"

            # 1. Initialize a dummy Proton Git repo so internal `cd Proton` checks pass cleanly
            mkdir -p Proton
            git -C Proton init -q
            git -C Proton config user.name "CI"
            git -C Proton config user.email "ci@example.com"
            git -C Proton commit --allow-empty -m "init" -q
            git -C Proton checkout -b experimental_8.0 -q 2>/dev/null || true

            # 2. Prevent proton-tkg.sh from trying to fetch/overwrite the dummy Proton repo
            sed -i "s|git clone.*Proton.*|true|g" proton-tkg.sh

            # 3. Export build environment variables
            export _NOLIB32="wow64"
            export _no_steampath="y"
            export _steamvr_support="false"
            export _use_staging="false"
            export _build_gstreamer="false"
            export _build_mediaconv="false"
            export _ci_build="true"
            export MAKEFLAGS="-j$(nproc)"

            # GCC 14 CFLAGS compatibility for older C call sites in Wine
            export CFLAGS="-Wno-error=implicit-function-declaration -Wno-error=implicit-int -Wno-error=int-conversion -Wno-error=incompatible-pointer-types"

            # 4. Stub out Linux Steam bridge build functions at definition (return 0 immediately)
            sed -i "s/^function build_vrclient {/function build_vrclient { return 0;/" proton-tkg.sh
            sed -i "s/^function build_lsteamclient {/function build_lsteamclient { return 0;/" proton-tkg.sh
            sed -i "s/^function build_steamhelper {/function build_steamhelper { return 0;/" proton-tkg.sh
            sed -i "s/^function build_mediaconverter {/function build_mediaconverter { return 0;/" proton-tkg.sh

            # 5. Preserve valid Bash syntax in GStreamer move/strip conditionals
            sed -i "s|strip --strip-unneeded \"\$_nowhere\"/gst.*|true|g" proton-tkg.sh
            sed -i "s|mv \"\$_nowhere\"/gst.*|true|g" proton-tkg.sh

            # 6. Teach proton-tkg.sh that _NOLIB32="wow64" skips 32-bit library packaging paths
            sed -i "s/if \[ \"\$_NOLIB32\" != \"true\" \]; then/if [ \"\$_NOLIB32\" != \"true\" ] \&\& [ \"\$_NOLIB32\" != \"wow64\" ]; then/g" proton-tkg.sh

            # 7. Write configuration overrides
            # _protonify="true" keeps Proton game patches active (fsync, game fixes, etc.)
            # _unfrog="true" skips ONLY the broken linux-steamclient bridge swap patch family
            mkdir -p proton-tkg-profiles
            {
              printf "%s\n" "_LOCAL_PRESET=\"none\""
              printf "%s\n" "_custom_wine_source=\"https://gitlab.winehq.org/wine/wine.git\""
              printf "%s\n" "_plain_version=\"\""
              printf "%s\n" "_use_staging=\"false\""
              printf "%s\n" "_protonify=\"true\""
              printf "%s\n" "_proton_use_steamhelper=\"false\""
              printf "%s\n" "_proton_fsync=\"true\""
              printf "%s\n" "_unfrog=\"true\""
              printf "%s\n" "_NOLIB32=\"wow64\""
              printf "%s\n" "_no_steampath=\"y\""
              printf "%s\n" "_no_container=\"true\""
              printf "%s\n" "_ci_build=\"true\""
              printf "%s\n" "_steamvr_support=\"false\""
              printf "%s\n" "_build_gstreamer=\"false\""
              printf "%s\n" "_build_mediaconv=\"false\""
              printf "%s\n" "uninstaller=\"true\""
              printf "%s\n" "autoinstall=\"true\""
            } | tee -a proton-tkg.cfg proton-tkg-profiles/advanced-customization.cfg > /dev/null

            # Execute build
            set -o pipefail
            yes | ./proton-tkg.sh nocontainer 2>&1 | tee build.log
            build_rc=${PIPESTATUS[1]}

            # Record checked out Wine commit
            _wdir=$(find . -maxdepth 1 -iname "wine-*" -type d 2>/dev/null | head -n1)
            if [ -n "$_wdir" ] && [ -e "$_wdir/.git" ]; then
              git -C "$_wdir" log -1 --oneline > wine_commit_actually_checked_out.log 2>&1 || true
            fi

            exit $build_rc
          '

      - name: Show which wine commit was actually used
        if: always()
        run: |
          echo "### Wine commit actually checked out" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          cat proton-tkg/wine_commit_actually_checked_out.log >> "$GITHUB_STEP_SUMMARY" \
            || echo "File not found -- wine source directory never appeared." >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"

      - name: Show build errors on failure
        if: failure()
        run: |
          echo "### Build Failure Summary" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          grep -nEi "error:|Error [0-9]+|FAILED:|fatal error|undefined reference" \
            proton-tkg/build.log | tail -150 >> "$GITHUB_STEP_SUMMARY" \
            || echo "No matching error lines found." >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"

      - name: Upload full build log
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: proton-tkg-master-wow64-nosteam-build-log
          path: proton-tkg/build.log

      - name: Upload Proton Artifact
        if: success()
        uses: actions/upload-artifact@v7
        with:
          name: proton-tkg-master-wow64-nosteam
          path: proton-tkg/built/**
